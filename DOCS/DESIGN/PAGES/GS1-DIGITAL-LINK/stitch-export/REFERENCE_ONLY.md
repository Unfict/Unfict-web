# REFERENCE ONLY --- Raw Stitch Export

`code.html` and `DESIGN.md` are preserved verbatim as source evidence.

They are not production implementation authority.

## Do Not Import Mechanically

The raw export contains generated choices that conflict with or exceed
current authority, including:

### Framework / implementation

-   Tailwind CDN;
-   generated Tailwind configuration;
-   Material Symbols;
-   remote Google-hosted image assets;
-   page-local Header/Footer implementations;
-   generated inline icon SVGs.

### Brand drift

-   IBM Plex Serif as a display/headline typeface;
-   generated blues such as `#0053CE` and `#266CF3`;
-   generated neutral palette values that do not replace Brand v3
    tokens;
-   typeset `UNFICT` in place of the proprietary wordmark;
-   decorative shadow/blur choices that require Brand normalization.

Brand v3 remains controlling:

``` text
IBM Plex Sans — primary typeface, including headings
IBM Plex Mono — machine identity only
#1965EC — canonical Unfict Blue
#0A0D14 — Deep Ink
#596273 — Slate
#DDE1E7 — Border Gray
#F6F7F9 — Institutional Gray
#FFFFFF — White
```

### Generated technical/content claims requiring removal, neutralization, or authority verification

Examples in the raw export include:

-   `GS1 Digital Link 1.2`;
-   `ISO/IEC 18004` / exact carrier-standard labeling;
-   `5 to 20+ years`;
-   `12 to 36 months`;
-   `https://id.unfict.com/...` as if canonical;
-   `immutable product identifier authoritative across retail systems`;
-   `EU Digital Product Passport` as an example current resource;
-   dispatch criteria such as language, device context, user-agent, and
    authorization token;
-   `SLA: < 28ms Edge Execution`;
-   `high-availability infrastructure strictly compliant with published global specs`;
-   `GS1 DIGITAL LINK v1.2 · 2026 ROADMAP`;
-   `Direct API spec alignment and schema definitions`;
-   Footer claims and routes such as `GS1 Global Conformance`,
    `Security & Encryption`, `Authoritative Root Registry`, and
    `< 28ms Global SLA`.

None of these become authoritative because they appear in Stitch.

## What May Be Extracted

Extract the accepted visual relationships and recurring Jobs after
normalization to governing Brand/content/semantic authority.

Examples:

-   dark Reality-first Hero relationship;
-   primary + subordinate Hero action hierarchy;
-   bounded conceptual flow;
-   restrained white/gray section rhythm;
-   progressive depth cards;
-   centered final conversion closure;
-   section numbering/eyebrow grammar as a visual observation.

Do not extract unsupported semantics along with the visuals.
